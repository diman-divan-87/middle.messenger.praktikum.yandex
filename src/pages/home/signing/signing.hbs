<div class="container">
    <Form class="login-form login-form--signing">
        <div class="login-form-content">
            <div class="title">Регистрация</div>
            <div class="input-block">{{> Input label="Почта" }}</div>
            <div class="input-block">{{> Input label="Логин" }}</div>
            <div class="input-block">{{> Input label="Имя" }}</div>
            <div class="input-block">{{> Input label="Фамилия" }}</div>
            <div class="input-block">{{> Input label="Телефон" }}</div>
            <div class="input-block">{{> Input label="Пароль" }}</div>
            <div class="input-block">{{> Input label="Пароль (еще раз)" }}</div>
        </div>
        <div class="login-form-content">
            {{> Button label="Зарегистрироваться" type="primary" }}
            {{> Button label="Войти" type="link" }}
        </div>
    </Form>
</div>
